#include <iostream>

using namespace std;

int main() {
    int t;
    cin >> t;
    for(int i = 0;i<t;i++) {
        int lower, upper, middle;
        cin >> lower >> upper;
        lower++;
        middle = (lower+upper)/2
        while(lower != upper) {
            
        }
    }
}